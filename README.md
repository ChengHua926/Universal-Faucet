# Universal Faucet CLI

This repo owns the miner client only:

```text
drip CLI
  -> local EVM identity
  -> bundled/managed XMRig
  -> external Stratum pool/backend
  -> local cumulative voucher cache
  -> claim/withdraw handoff UX
```

Out of scope here:

```text
backend
Stratum gate
XMRig Proxy
Postgres accounting
share validation
contracts
Crossroads routing/swap/bridge
relayer service
```

The backend/API is owned elsewhere. `drip` talks to it through:

```text
GET  /miner/:addr
GET  /pool
GET  /state/:addr
POST /voucher
POST /restore
```

`addr` is the local Ethereum address generated by the CLI. It is also the
XMRig username.

## Commands

```bash
cargo run -p drip-cli -- identity
cargo run -p drip-cli -- start --threads 2
cargo run -p drip-cli -- status
cargo run -p drip-cli -- checkpoint
cargo run -p drip-cli -- restore
cargo run -p drip-cli -- claim --refresh
cargo run -p drip-cli -- withdraw base-sepolia eth 0x1111111111111111111111111111111111111111 --refresh
cargo run -p drip-cli -- stop
```

Useful local env:

```bash
export DRIP_HOME=/private/tmp/drip-demo
export DRIP_API_BASE_URL=https://p8080.m269.opf-mainnet-rofl-55.rofl.app
export DRIP_POOL_URL=stratum+ssl://p3333.m269.opf-mainnet-rofl-55.rofl.app:443
export DRIP_POOL_TLS=false
# Optional fallback: operator relay.
# export DRIP_POOL_URL=<operator-clearnet-relay>:3333
# Optional fallback: Tor onion stratum endpoint.
# export DRIP_POOL_URL=vj3o34twitcqk7jxopms5mpoxeurqjfdpvlpnxgmkveld3nggmzsmtid.onion:3333
# export DRIP_TOR_SOCKS5=socks5://localhost:9050
# export DRIP_XMRIG_PATH=/absolute/path/to/xmrig
```

Local files:

```text
$DRIP_HOME/config.json        EVM address/private key + API/pool config
$DRIP_HOME/voucher.json       latest cumulative voucher only
$DRIP_HOME/xmrig-config.json  generated XMRig config
$DRIP_HOME/xmrig.pid          local XMRig PID
$DRIP_HOME/xmrig.log          XMRig log
$DRIP_HOME/voucher-loop.pid   background voucher checkpoint helper PID
$DRIP_HOME/voucher-loop.log   voucher checkpoint helper log
```

## XMRig

Users do not install XMRig. The release package bundles a source-built XMRig
binary.

The generated XMRig config uses:

```text
pool url = config.mining_pool_url
user     = local Ethereum address
pass     = x
rig-id   = local Ethereum address
sni      = true for rofl.app pool hosts
socks5   = config.tor_socks5, when set
cpu.rx   = one -1 affinity entry per requested thread
```

For the ROFL faucet pool, the production Stratum URL is
`stratum+ssl://p3333.m269.opf-mainnet-rofl-55.rofl.app:443`. `drip` enables
SNI in the generated XMRig config for `rofl.app` hosts. Relay and Tor endpoints
remain fallback paths.

Package XMRig:

```bash
DRIP_XMRIG_PLATFORM=darwin-arm64 scripts/package-xmrig.sh
```

Package `drip` with bundled XMRig:

```bash
DRIP_XMRIG_PLATFORM=darwin-arm64 scripts/package-xmrig.sh
DRIP_XMRIG_PLATFORM=darwin-arm64 scripts/package-drip.sh
```

## Voucher Model

While mining, `drip` runs a background checkpoint helper. Every configured
interval, default 300 seconds, it calls:

```http
POST /voucher
{ "user": "0x<addr>", "amount": null }
```

The response is cumulative. The CLI stores exactly one voucher: the highest
`cumulative_amount` seen.

Stored voucher shape:

```json
{
  "user": "0x...",
  "cumulative_amount": "12345",
  "signed_at": 1780000000,
  "signature": "0x..."
}
```

`drip restore` replays that voucher to `POST /restore`.

## Docs

Read [docs/handoff.md](docs/handoff.md) for the current handoff.
Read [docs/faucet-integration.md](docs/faucet-integration.md) for the faucet
API/Tor integration boundary.
Read [docs/demo-playbook.md](docs/demo-playbook.md) for demo install/run steps.
Read [docs/cli-ux.md](docs/cli-ux.md) for CLI UX rules and demo generation.

## Verification

```bash
cargo test --workspace
cargo run -p drip-cli -- --help
```
